#include <iostream>
using namespace std;
int main()
{
    int number = 1; // start point
    while(number <= 5) // test to end the loop
	{ 
        cout<<"number is "<<number<<endl;
        number=number+1; // number to increment by
    }
    return 0;
}
