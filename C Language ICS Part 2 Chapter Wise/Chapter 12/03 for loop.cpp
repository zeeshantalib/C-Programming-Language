#include<stdio.h>
#include<conio.h>
int main()
{
	int n;
	// here variable "n" is declared.
	for(n=1;n<=5;n++)
	/* the main difference between the while and for loop is , The initialization ,
	  the  condition and the increment/decrement operator are written in the
	same or single  expression .  */ 
	// for loop also repeat statement until the condition is true.
		printf("pakistan \n");
	getch();
}
