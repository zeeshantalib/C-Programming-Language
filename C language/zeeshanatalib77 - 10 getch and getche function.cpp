#include<stdio.h>
#include<conio.h>
int main()
{
	printf("hello world");
	getch();
	// the getch only gets character from the user.and terminates program on pressing any key.
	// it is defined in header file conio.h
	printf("\nhello");
	getche();
	/*  the getceh stands for "get character echo".it terminates the program with displaying the key presssed on the DOS
	window*/ 
}

